<li class="nav-section">
    @if (isset($icon))
        <span class="sidebar-mini-icon">
            <i class="{{ $icon }}"></i>
        </span>
    @endif
    <h4 class="text-section">{{ $title }}</h4>
</li>
